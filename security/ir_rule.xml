<odoo>
    <record id="sale_users_own_orders" model="ir.rule">
        <field name="name">Sale Users Own Orders Rule</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_salesman'))]"/>
    </record>

    <!-- A rule for sales manager to make them see all orders as they inherit from group_sale_salesman -->
    <record id="sale_manager_all_orders" model="ir.rule">
        <field name="name">Sale Manager All Orders Rule</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
    </record>
</odoo>